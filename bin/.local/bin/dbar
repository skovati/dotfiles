#!/bin/sh
# outputs basic info to xroot for the dwm status bar

SEP=" | "

status() {
    printf " %s$SEP%s$SEP%s$SEP%s$SEP%s$SEP%s" "cpu: $(dcpu)" "bat: $(dbat)" "vol: $(dvol)" "wg: $(dwg)" "mem: $(dmem)" "$(dtime)"
}

while true; do
    xsetroot -name "$(status)"
    sleep 1
done
